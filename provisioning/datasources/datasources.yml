apiVersion: 1

datasources:
  - name: 'grail'
    type: 'discostu105-grail-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      tenantUrl: '${DT_TENANT_URL}'
    secureJsonData:
      apiToken: '${DT_TOKEN}'
